#include <stdio.h>

int main(){

    double notas[2][4];

    for(int aluno = 0; aluno <= 1;aluno++)
    printf("Insira as notas do aluno %d:\n" aluno + 1);
           for(posNotas = 0; posNotas < 4;posNotas++){
        printf("Insira a nota do %d:", i + 1, notas[0][posNotas]);
        scanf("%lf", &notas[aluno][posNotas]);
    }


    return 0;
}