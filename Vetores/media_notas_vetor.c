#include <stdio.h>

int main(){
    double notas[4], somaDasNotas = 0;
    char nomeAluno[20]; // {'g','u','s','t','a','v','o'}; Feio //

    scanf("%s", &nomeAluno);
    printf("%s", nomeAluno);

    return 0;
}